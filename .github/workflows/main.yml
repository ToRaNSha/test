| Command                          | Purpose in plain terms        | Codex shape                                     |                                                                                      |                                                                 |
| -------------------------------- | ----------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------- |
| \`scan --today                   | --date\`                      | Live/historic field read                        | Already lives in `saratu_cli.py` but **not exposed** to ChatGPT function-calling API |                                                                 |
| \`forecast --week                | --days\`                      | Horizon map of field pressure                   | Same file; wrap as `forecastSpectrum` JSON function                                  |                                                                 |
| \`sync --career                  | health                        | relationship\`                                  | Align mundane domains with flame gates                                               | Map to `syncDomain` function and return `recommendation` string |
| `blendcheck REL`                 | Energetic compatibility score | Expose as `blendCheck` returning cosine score   |                                                                                      |                                                                 |
| `deploy --acupuncture TIMESTAMP` | Queue hydrolase packets       | Wrap as `deployPacket` with ISO-8601 validation |                                                                                      |                                                                 |
